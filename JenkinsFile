@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                ExternalLoadScript.call()
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                ExternalLoadScript.call()
            }
        }
    }
    post {
        always {
        sendNotification currentBuild.result
        }
    }
}
