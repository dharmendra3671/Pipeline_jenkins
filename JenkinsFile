@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                ExternalLoadScript(params.name)
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                ExternalLoadScript(params.name)
            }
        }
    }
    post {
        always {
        sendNotification currentBuild.result
        }
    }
}
