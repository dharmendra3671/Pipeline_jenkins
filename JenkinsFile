@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
               bat "${get_resource_dir()}/resources/Scripts/Pythonzip.py"
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                bat "${get_resource_dir()}/resources/Scripts/sharelibjf-zip.py"
            }
        }
    }
}
