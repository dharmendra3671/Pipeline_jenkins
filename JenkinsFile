@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                ExternalLoadScript 
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                ExternalLoadScript 
            }
        }
    }
    post {
        always {
        sendNotification currentBuild.result
        }
    }
}
