@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                ExternalLoadScript("Pythonzip.py")
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                ExternalLoadScript("sharelibjf-zip.py")
            }
        }
    }
    post {
        always {
        sendNotification currentBuild.result
        }
    }
}
