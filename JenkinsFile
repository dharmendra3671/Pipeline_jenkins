@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                ExternalLoadScript.call(${config.name})
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                ExternalLoadScript.call(${config.name})
            }
        }
    }
    post {
        always {
        sendNotification currentBuild.result
        }
    }
}
