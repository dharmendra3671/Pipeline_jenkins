@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                loadScript.call(${config.name})
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                loadScript.call(${config.name})
            }
        }
    }
    post {
        always {
        sendNotification currentBuild.result
        }
    }
}
