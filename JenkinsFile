@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
               bat "get_resource_dir().call()"
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                //echo 'deploying'
                bat "get_resource_dir().call()"
            }
        }
    }
}
