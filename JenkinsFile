@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
               bat "${get_resource_dir()}/Pythonzip.py"
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                bat "${get_resource_dir()}/sharelibjf-zip.py"
            }
        }
    }
}
