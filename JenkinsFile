@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script{
                    get_resource_dir.get_resource()
                }
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                echo 'deploying'
               // bat "get_resource_dir().call()"
            }
        }
    }
}

