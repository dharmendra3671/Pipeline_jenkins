@Library('mypipeline_notification') _

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
               bat {ExternalLoadScript()}/Scripts 
            }
        }
        stage ('test') {
            steps {
                echo 'testing'
            }
        }
        stage('deploy'){
            steps {
                bat {ExternalLoadScript()}/Scripts
        }
    }
    post {
        always {
        sendNotification currentBuild.result
        }
    }
}
